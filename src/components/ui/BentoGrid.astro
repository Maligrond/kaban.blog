---
import { cn } from "@/lib/utils";

export interface Props {
  class?: string;
}

const { class: className } = Astro.props as Props;
---
<div class={cn("grid w-full grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3", className)}>
  <slot />
  
</div>


