# Блог Кабана (kaban.blog)

## Обзор проекта

Персональный блог на русском языке, построенный на [Astro Micro](https://astro-micro.vercel.app/) — форке темы [Astro Nano](https://astro-nano-demo.vercel.app/). Проект использует статическую генерацию сайтов (SSG) с Astro.

### Основные функции
- 📝 **Блог** — статьи с поддержкой тегов и MDX
- 🚀 **Проекты** — портфолио с ссылками на демо и репозитории
- 📚 **Книги** — прочитанные книги с рейтингами
- 🎮 **Игры** — пройденные игры с рейтингами
- 🎬 **Фильмы** — просмотренные фильмы с рейтингами
- 🌍 **Страны** — посещённые страны с рейтингами
- 🔍 **Поиск** — Pagefind для полнотекстового поиска
- 💬 **Комментарии** — Giscus для обсуждений
- 🌓 **Темы** — System/Light/Dark режимы

---

## Технологический стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| Astro | ^5.2.5 | Фреймворк для сайта |
| Tailwind CSS | ^3.4.16 | Стилизация |
| TypeScript | ^5.7.2 | Типизация |
| React | ^19.2.0 | Интерактивные компоненты |
| MDX | ^4.0.1 | Расширенный Markdown |
| Pagefind | ^1.2.0 | Поиск по сайту |
| Decap CMS | ^3.8.4 | Управление контентом |
| Netlify Identity | — | Авторизация для CMS |

---

## Структура проекта

```
kaban.blog/
├── public/                     # Статические файлы (изображения, favicon)
├── src/
│   ├── components/             # Astro/React компоненты
│   │   ├── ArrowCard.astro     # Карточка для постов
│   │   ├── Footer.astro        # Подвал сайта
│   │   ├── Header.astro        # Навигация
│   │   ├── Head.astro          # SEO meta-теги
│   │   ├── PageFind.astro      # Поисковый виджет
│   │   ├── PostNavigation.astro # Навигация prev/next
│   │   ├── TableOfContents.astro # Оглавление
│   │   ├── Giscus.astro        # Комментарии
│   │   ├── Callout.astro       # Выноски/заметки
│   │   └── ui/                 # shadcn/ui компоненты
│   ├── content/                # Контент-коллекции
│   │   ├── blog/               # Посты (17 файлов)
│   │   ├── projects/           # Проекты (4 файла)
│   │   ├── books/              # Книги (15 файлов)
│   │   ├── games/              # Игры (8 файлов)
│   │   ├── movies/             # Фильмы (8 файлов)
│   │   └── countries/          # Страны (58 файлов)
│   ├── layouts/
│   │   ├── Layout.astro        # Базовый layout
│   │   └── BlogPost.astro      # Layout для постов
│   ├── pages/                  # Маршруты Astro
│   │   ├── index.astro         # Главная страница
│   │   ├── blog/               # Страницы блога
│   │   ├── projects/           # Страницы проектов
│   │   ├── books/              # Страницы книг
│   │   ├── games/              # Страницы игр
│   │   ├── movies/             # Страницы фильмов
│   │   ├── countries/          # Страницы стран
│   │   ├── tags/               # Страницы тегов
│   │   ├── about/              # О себе
│   │   ├── bookmarks.astro     # Закладки
│   │   ├── admin/              # Decap CMS
│   │   └── rss.xml.js          # RSS-лента
│   ├── styles/                 # Глобальные стили
│   ├── consts.ts               # Конфигурация сайта
│   ├── content.config.ts       # Схема коллекций Astro 5
│   └── types.ts                # TypeScript типы
├── astro.config.mjs            # Конфигурация Astro
├── tailwind.config.mjs         # Конфигурация Tailwind
├── netlify.toml                # Деплой на Netlify
├── .pages.yml                  # Конфигурация CMS
└── package.json                # Зависимости
```

---

## Команды разработки

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера (http://localhost:4321)
npm run dev

# Сборка для продакшена
npm run build

# Превью собранного сайта
npm run preview

# Проверка TypeScript
npx astro check
```

---

## Контент-коллекции (Content Collections)

Проект использует Astro Content Collections. Схемы определены в `src/content.config.ts`:

### Blog
```typescript
{
  title: string;           // Заголовок поста
  description: string;     // Описание
  date: Date;              // Дата публикации
  draft?: boolean;         // Черновик (не публикуется)
  tags?: string[];         // Теги
  previewImage?: string;   // Превью изображение
}
```

### Projects
```typescript
{
  title: string;
  description: string;
  date: Date;
  draft?: boolean;
  demoURL?: string;        // Ссылка на демо
  repoURL?: string;        // Ссылка на репозиторий
}
```

### Books
```typescript
{
  title: string;
  author: string;
  date: string;
  rating: number;          // 1-5
  url?: string;
}
```

### Games / Movies
```typescript
{
  title: string;
  year: number;
  date: string;
  rating: number;          // 1-5
  url?: string;
}
```

### Countries
```typescript
{
  title: string;
  country: string;
  date: string;
  rating: number;          // 1-5
  flag: string;            // Эмодзи или путь к изображению
  favorite?: boolean;
}
```

---

## Конфигурация сайта

Файл `src/consts.ts`:

```typescript
export const SITE = {
  TITLE: "Блог Кабана",
  DESCRIPTION: "О моей жизни и опыте.",
  EMAIL: "kaban@gmail.com",
  NUM_POSTS_ON_HOMEPAGE: 5,
  NUM_PROJECTS_ON_HOMEPAGE: 3,
};
```

---

## Стилизация

- **Tailwind CSS 3** с плагином `@tailwindcss/typography`
- Шрифты: `Geist Sans` и `Geist Mono` (через @fontsource)
- Темы: `darkMode: "class"` — переключение class-based
- Стили определены в `src/styles/global.css`

---

## Деплой

### Netlify (текущий)
Конфигурация в `netlify.toml`:
```toml
[build]
  command = "npm run build"
  publish = "dist"
```

### Decap CMS
- Админка доступна по `/admin`
- Авторизация через Netlify Identity
- Конфигурация коллекций в `.pages.yml`

---

## Ключевые файлы для редактирования

| Задача | Файл(ы) |
|--------|---------|
| Изменить метаданные сайта | `src/consts.ts` |
| Добавить пост | `src/content/blog/*.md` |
| Добавить проект | `src/content/projects/*.md` |
| Изменить навигацию | `src/components/Header.astro` |
| Изменить footer | `src/components/Footer.astro` |
| Изменить главную страницу | `src/pages/index.astro` |
| Добавить новый тип контента | `src/content.config.ts` + `src/content/[type]/` |
| Настроить SEO | `src/components/Head.astro` |
| Добавить страницу | `src/pages/[name].astro` |

---

## Рекомендации для AI-агентов

### При работе с контентом
1. Новые посты создавать в `src/content/blog/` как `.md` или `.mdx` файлы
2. Соблюдать схему frontmatter из `src/content.config.ts`
3. Изображения для постов размещать в `public/`

### При работе с компонентами
1. Компоненты Astro имеют расширение `.astro`
2. Используется TypeScript — проверять типы перед коммитом
3. Стили писать с Tailwind CSS классами

### При добавлении функций
1. Новые страницы — в `src/pages/`
2. Новые API-маршруты — `src/pages/*.ts` (если нужны)
3. Для интерактивности — React-компоненты с директивой `client:*`

### Важные особенности Astro 5
- Используется `glob()` loader для контент-коллекций
- Типы импортируются через `import type`
- Маршрутизация файловая: `pages/blog/[...slug].astro`

---

## TODO (актуальные задачи)

Файл `TODO.txt` содержит выполненные задачи. Новые задачи добавлять сюда или в Issues.

---

## Ссылки

- [Документация Astro](https://docs.astro.build)
- [Astro Micro тема](https://astro-micro.vercel.app/)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Pagefind](https://pagefind.app)
- [Giscus](https://giscus.app)
- [Decap CMS](https://decapcms.org)
